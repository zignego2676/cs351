the Makefile expects main.c, permutations.c, permutations.h, and itself to be in the same directory
also for longer words, the permutations function has O(n^2) time so it will take a while but it is running
